<!DOCTYPE html>
<html>
  <head>
    <title>PlayCmd</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Mulish:wght@300;400;700&display=swap" rel="stylesheet">

    <script src="//cdn.jsdelivr.net/npm/canvas-confetti@1.4.0/dist/confetti.browser.min.js"></script>
    <script src="//cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    <script src="//unpkg.com/alpinejs" defer></script>
    <link rel="stylesheet" href="//cdnjs.cloudflare.com/ajax/libs/highlight.js/11.2.0/styles/default.min.css">
    <script src="//cdnjs.cloudflare.com/ajax/libs/highlight.js/11.2.0/highlight.min.js"></script>

    <%= stylesheet_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body class="bg-background">
    <div>
      <% if notice %>
        <%= render partial: "layouts/notice", object: notice %>
      <% end %>
      <% if alert %>
        <%= render partial: "layouts/alert", object: alert %>
      <% end %>

      <div class="flex h-screen overflow-hidden bg-gray-100 dark:bg-darkBackground">
        <div class="hidden md:flex md:flex-shrink-0">
          <%= render partial: "layouts/navigation" %>
        </div>
        <div class="flex flex-col flex-1">
          <div class="w-full px-6 mx-auto mt-4 md:px-8">
            <div class="relative z-10 flex flex-shrink-0 h-16">
              <button class="px-4 text-gray-500 border-r border-gray-200 focus:outline-none focus:ring-2 focus:ring-inset focus:ring-pink-500 md:hidden">
                <span class="sr-only">Open</span>
                <svg class="w-6 h-6" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor" aria-hidden="true">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h7"/>
                </svg>
              </button>
              <div class="flex justify-between flex-1 px-4 md:px-0">
                <div class="flex justify-between flex-1">
                  <div class="hidden w-1/3 mt-0 sm:mt-4 md:block">
                    <label for="search" class="sr-only">Search for other people or skills!</label>
                    <div class="relative text-gray-400 focus-within:text-gray-400">
                      <%= render partial: "search/search_form" %>
                    </div>
                  </div>
                </div>
                <div class="flex items-center ml-4 md:ml-6">
                  <div class="flex px-2 mt-0 sm:mt-4 sm:ml-4">
                    <% if !current_page?(:controller => 'posts', :action => 'new') %>
                      <%= link_to "Uploaden", new_post_path, class:'inline-flex invisible lg:visible items-center py-2 text-sm font-medium text-white border border-none rounded-md shadow-sm px-9 hover:text-gray-200 bg-gradient-to-r from-green-400 to-blue-500 hover:bg-gray-900 focus:outline-none ring-0 sm:order-1 sm:ml-3' %>
                    <% end %>
                  </div>
                </div>
              </div>
            </div>
          </div>
          <div class="flex items-stretch flex-1 overflow-hidden">
            <main class="flex-1 px-6 mt-8 overflow-scroll md:px-10 focus:outline-none">
              <%= yield %>
            </main>
          </div>
        </div>
      </div>
    </div>
  </body>
</html>
